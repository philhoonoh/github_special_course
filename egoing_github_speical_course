egoing github 특강!

1. U : untrakced files, M : Modified Files

2. git commit -am "" 
	Tracked file 만 commit
	configuration upload 위험 방지 

3. Untracked file 경우 
	git add FILENAME 
	git add . -> 나쁜 습관

4. system 상에서 Untracked file 설정
	$ vi .git/info/exclude 에 추가

	FILENAME
	
	Ref.
		https://docs.microsoft.com/ko-kr/azure/devops/repos/git/ignore-files?view=azure-devops&tabs=visual-studio

5. vscode 에서 숨긴 파일 설정

	1) 직접설정
		Code → Preference → Setting -> Search for files.exclude -> .git 제거

	2) 파일에서 설정
		.vscode/settings.json
		{
    		"editor.fontSize": 13,
    		"files.exclude": {
        		"**/.git": false
    		}
		}

	Ref.
		https://stackoverflow.com/questions/40818354/visual-studio-code-git-folder-file-hidden

=====

6. git log --oneline
7. Git Graph Extension
	git log --pretty=format:"%h %s" --graph

8. commit 이름 바꾸기
	git commit --amend -m "rename commit"
	* commit ID 가 달라진다. 
	* HEAD - MASTER : 
		HEAD -> last commit
		MASTER -> branch

	amend 전 : a <- b <- c
		HEAD - MASTER : c 를 가리킨다.

	amend 후 : a <- b <- c2, c 는 사라짐 
		HEAD - MASTER : c2 를 가리킨다.

9. git checkout HashID(4 digit)
	* 항상 HEAD 를 움직인다 (시간여행)
	HASH commit에 HEAD를 이동
	git checkout main 
		: main 으로 다시 이동

	* Detached HEAD State -> 경고를 준다.
		HEAD하고 MASTER하고 일치하지 않은 상태에서 commit을 할 경우 

		A <- B(master) <- C (Head)

		이 상태에서 git checkout master 할경우

		A <- B(master,head) <- C (날라간다)

		master를 C로 옮길려면?

		A <- B <- C (master,head) 할려면?

10. git reset --hard 
	* HEAD가 가리키는 곳으로 branch 를 움직인다.

11. git reflog
	* 이전 모든 history 



